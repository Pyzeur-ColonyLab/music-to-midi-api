# Server Configuration
PORT=8000
HOST=0.0.0.0

# Model Loading
# Set to 1 to skip model loading (testing mode)
SKIP_MODEL_LOADING=0

# Auto-reload (development mode)
# Set to 1 to enable auto-reload on file changes
ENABLE_RELOAD=0

# Processing Configuration
# Set to 1 to bypass Demucs stem separation and transcribe directly with YourMT3
# When enabled, YourMT3 processes the full audio file instead of separated stems
# This is faster but may reduce transcription quality for multi-instrument tracks
BYPASS_DEMUCS=0

# Upload Limits
# Maximum upload size in MB (default: 500MB for music production files)
MAX_UPLOAD_SIZE_MB=500

# Security
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8000

# Performance
REQUEST_TIMEOUT=600
